#  Application
base.url=https://www.saucedemo.com

#  Browser
# Overridden at runtime via -Dbrowser=firefox
browser=chrome

# Headless mode
# Set to true in CI via -Dheadless=true
headless=false

#  Timeouts (seconds)
implicit.wait=10
page.load.timeout=30